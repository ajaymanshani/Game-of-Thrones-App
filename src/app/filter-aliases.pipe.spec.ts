import { FilterAliasesPipe } from './filter-aliases.pipe';

describe('FilterAliasesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAliasesPipe();
    expect(pipe).toBeTruthy();
  });
});
